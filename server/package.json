{
  "name": "lexicloud-server",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Self-hosted backend for LexiCloud (replaces the previous Lovable/Supabase backend). Auth, database, and AI generation all run on your own server now.",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "dependencies": {
    "better-sqlite3": "^11.3.0",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5"
  },
  "engines": {
    "node": ">=18"
  }
}
